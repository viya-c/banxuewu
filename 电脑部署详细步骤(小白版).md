# 星光伴学屋 · 电脑部署详细步骤（技术小白版）

> 目标：把「星光伴学屋」变成一个**全家都能打开的免费网址**。
> 你只需要：一台电脑（Windows / Mac 都行）、一个邮箱、约 15 分钟。
> 不用装编程软件、不用记命令。全程“点点点”就行。

---

## 先拿到代码（从本聊天下载）

在本对话里，我给你发了两个文件卡片：

| 文件 | 什么时候用 |
|---|---|
| **`xingguang-lite.zip`**（极简 3 文件，推荐新手） | 走「方案 A：Koyeb」最简单路径用这个 |
| **`xingguang-source.zip`**（完整源码） | 走「方案 B：Cloudflare」进阶路径用这个 |

👉 点文件卡片上的 **下载** 按钮，保存到电脑**桌面**。
👉 下载好后，**右键 → 解压**（Windows 选“解压到当前文件夹”；Mac 直接双击）。
👉 解压后得到一个文件夹（里面是 `server.js` 和一个 `public` 文件夹）。

> 推荐新手直接走 **方案 A（Koyeb）**，最简单、免费、不用绑银行卡。

---

# 方案 A（推荐·最简单）：GitHub 上传 + Koyeb 部署

这一个网址就能用，后端也包含在内，全家数据互通。免费、不绑卡。

## 第 1 步：注册 GitHub（只做一次）

1. 电脑浏览器打开 👉 **github.com**
2. 点右上角 **Sign up**（用邮箱注册），或点 **Continue with Google / Apple** 用现成账号。
3. 按提示验证邮箱，登录进去。

## 第 2 步：用 GitHub Desktop 把代码传上去（不用记命令）

1. 下载安装 **GitHub Desktop**：浏览器打开 👉 **desktop.github.com** → 点 **Download for …** → 安装。
2. 打开 GitHub Desktop → 用你的 GitHub 账号登录。
3. 顶部菜单 **File → Add Local Repository…（添加本地仓库）**
4. 在弹窗里点 **Choose…（选择）**，选你**刚解压的那个文件夹**（就是里面有 `server.js` 的那个）。
   - 它会提示“这不是一个 Git 仓库”，点 **Create a repository（创建仓库）** 即可。
5. 左上角会出现 **Publish repository（发布仓库）** 按钮，点它。
6. 填写：
   - **Name（名称）**：填 `xingguang`（随便起，英文）
   - **Description（描述）**：空着
   - **Keep this code private（私有）**：**不要勾**（要选 Public 公开放才能部署）
7. 点 **Publish repository** → 等进度条走完。
   ✅ 现在代码已经在你的 GitHub 上了。

## 第 3 步：用 Koyeb 部署成网站（免费·不绑卡）

1. 电脑浏览器打开 👉 **app.koyeb.com**（或 koyeb.com 点“控制台”）
2. 点 **Continue with GitHub**，用 GitHub 登录并**授权**。
3. 点 **Create App（创建应用）** → 选 **GitHub** → 选你刚建的 **`xingguang`** 仓库。
4. 在“构建与启动”设置里填两项（其它全默认）：
   - **Build command（构建命令）**：**留空**（零依赖，不用装包）
   - **Run command（启动命令）**：填 👉 **`node server.js`**
5. 点 **Deploy（部署）**。
6. 等 1～2 分钟，页面会出现一个网址，类似：
   `https://xxxx.koyeb.app`
   ✅ 这就是你的应用网址！

## 第 4 步：打开使用

1. 浏览器打开那个 `https://xxxx.koyeb.app` 网址。
2. 输入访问码 👉 **`2026`**
3. 选身份进入（管理员 / 学员 / 访客）。
   ✅ 能正常看到课表、作业、打卡星星，就成功了。

## 第 5 步：全家一起用

- 把 `https://xxxx.koyeb.app` 发到家庭群。
- 每个人用自己手机/电脑打开，输入访问码 `2026`，选不同身份，看到的**是同一份数据**。
- 打卡、记作业，全家人实时共享。

## 以后想更新代码怎么办？

1. 在 GitHub Desktop 里改完/替换文件后，左下角填一句说明，点 **Commit to main**（提交）。
2. 再点 **Push**（推送）到 GitHub。
3. 回到 Koyeb，点 **Redeploy（重新部署）** 即可（部分情况会自动重新部署）。

---

# 方案 B（进阶·永久免费 + 全家实时同步）：GitHub + Cloudflare

如果你想要“前端跑在 GitHub Pages、后端跑在 Cloudflare、永久免费、改完代码自动更新”，走这条。
这条比方案 A 多几步，需要建一个数据库、填两个密钥，但都有界面操作、不用写代码。

👉 详细图文步骤见仓库里的 **`deploy/cloudflare/DEPLOY.md`**（完整源码 zip 里也有）。

大体流程：
1. 用同样的方法把 **`xingguang-source.zip`** 解压、用 GitHub Desktop 传到 GitHub。
2. 在 Cloudflare 网页里建一个 **D1 数据库**，复制它的 `database_id`。
3. 在 GitHub 仓库 **Settings → Secrets** 里填两个密钥（`CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`）。
4. 改 `public/js/config.js` 填 Worker 地址。
5. 推送代码 → GitHub Actions **自动部署**前端和后端。
6. 前端网址：`https://你的用户名.github.io/xingguang/`

---

# 常见问题

**Q：网址打不开 / 显示错误？**
A：回到 Koyeb（方案 A）或 Cloudflare（方案 B）控制台，看部署日志有没有红色报错。最常见是“启动命令”没填 `node server.js`。

**Q：免费版会不会睡着？**
A：Koyeb 免费版通常不会休眠；Render 免费版会睡（首次打开慢几秒）。想要“永远秒开”可升级付费档（很便宜）。

**Q：数据会丢吗？**
A：数据存在部署平台上。重要时可在应用内「我的 → 数据备份」导出 JSON 留存。换平台/删服务前记得导出。

**Q：访问码是多少？**
A：默认 **`2026`**。管理员可在「设置」里改。

**Q：想在自己电脑先试试？**
A：可以。电脑装一个 [Node.js](https://nodejs.org)（官网下载，一路下一步），在代码文件夹里打开终端输入 `node server.js`，浏览器开 `http://localhost:3000` 即可。
